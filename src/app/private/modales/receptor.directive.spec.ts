import { ReceptorDirective } from './receptor.directive';

describe('ReceptorDirective', () => {
  it('should create an instance', () => {
    const directive = new ReceptorDirective();
    expect(directive).toBeTruthy();
  });
});
